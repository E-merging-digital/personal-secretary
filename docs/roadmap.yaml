schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-domain-vertical
  purpose: >-
    Complete Epic #35's deterministic internal activity domain while ordinary
    occurrences and effective responsibility remain calculated from Drupal-owned state.

active_tasks:
  - issue: 43
    purpose: >-
      Materialize ResponsibilityRule, ResponsibilityOverride and deterministic
      effective responsibility after the EffectiveOccurrence exception overlay.

planned_tasks:
  - purpose: >-
      Materialize PreparationRequirement and deterministic preparation eligibility
      under separate Project Lead authority.

deferred_decisions: []
