schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-domain-vertical
  purpose: >-
    Complete Epic #35's deterministic internal activity domain through reusable
    preparation requirements and calculated preparation eligibility.

active_tasks:
  - issue: 49
    purpose: >-
      Materialize PreparationRequirement and deterministic preparation eligibility
      from EffectiveOccurrence and EffectiveResponsibility.

planned_tasks: []

deferred_decisions: []
