schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: recurrence-primitive-finalization
  purpose: >-
    Finalize Epic #32 through the bounded Task #33 after Project Lead adoption of
    date_recur, before starting the first bounded domain implementation slice.

active_tasks:
  - issue: 33
    purpose: >-
      Complete the adopted date_recur Stage-B candidate and return it for final
      exact-head Project Lead review and terminalization.

planned_tasks:
  - purpose: >-
      After Epic #32 is terminal, execute the first bounded domain
      persistence/application slice under separate Project Lead authority.

deferred_decisions: []
