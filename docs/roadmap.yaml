schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: technical-drupal-bootstrap
  purpose: >-
    Materialize Epic #29 through the single active bootstrap Task #30: a
    reproducible Drupal 11 + DDEV foundation with exact-head technical evidence.

active_tasks:
  - issue: 30
    purpose: >-
      Materialize the Drupal 11 Composer project, DDEV runtime, platform baseline,
      DEV-only isolation and technical CI without domain implementation.

planned_tasks:
  - purpose: >-
      After Project Lead review and merge of the bootstrap, run a separate bounded
      runtime spike for the preferred recurrence primitive date_recur.

deferred_decisions: []
