schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: recurrence-primitive-validation
  purpose: >-
    Validate Epic #32 through the bounded Task #33 runtime spike for the preferred
    recurrence primitive before any final adoption or domain implementation.

active_tasks:
  - issue: 33
    purpose: >-
      Prove the date_recur backend contract with synthetic weekly, timezone/DST,
      bounded infinite recurrence, overlay and series-edit runtime evidence.

planned_tasks:
  - purpose: >-
      After Project Lead adopts or rejects the recurrence primitive, execute the
      first bounded domain persistence/application slice under separate authority.

deferred_decisions: []
