schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-domain-vertical-core
  purpose: >-
    Materialize Epic #35's first Drupal-owned domain core with Person, Household,
    revisionable ActivitySeries and bounded base-occurrence projection.

active_tasks:
  - issue: 37
    purpose: >-
      Materialize Person, Household and ActivitySeries through governed Drupal
      domain services and prove persisted bounded recurrence integration.

planned_tasks:
  - purpose: >-
      After this domain core is independently reviewed and terminalized,
      materialize ActivityException together with revision/effective-from
      semantics and orphan reconciliation under separate Project Lead authority.

deferred_decisions: []
