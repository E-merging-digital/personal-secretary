schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: commercial-multilingual-foundation
  purpose: >-
    Keep one stable English source language for distributable Drupal
    configuration before returning to the highest-value occurrence UX.

active_tasks:
  - issue: 60
    purpose: >-
      Lock the canonical repository configuration source language to English
      without prematurely enabling the full multilingual product stack.

historical_context:
  - issue: 58
    purpose: >-
      Terminal occurrence cancellation established the governed path for
      cancelling one exact upcoming base occurrence.

planned_tasks:
  - issue: 61
    purpose: >-
      Resume the occurrence UX with rescheduling only after the configuration
      language foundation from issue 60 is terminal.

deferred_decisions: []
