schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-domain-vertical
  purpose: >-
    Complete Epic #35's deterministic internal activity domain while ordinary
    occurrences remain calculated from Drupal-owned state.

active_tasks:
  - issue: 40
    purpose: >-
      Materialize ActivityException, explicit effective-from ActivitySeries
      revisions, orphan detection, audited reconciliation and effective
      occurrence projection.

planned_tasks:
  - purpose: >-
      Materialize ResponsibilityRule, ResponsibilityOverride and deterministic
      effective responsibility under separate Project Lead authority.

deferred_decisions: []
