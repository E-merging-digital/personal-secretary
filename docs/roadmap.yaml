schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-user-facing-vertical
  purpose: >-
    Make Epic #51's terminal read-only #52 surface independently usable through
    the smallest governed first-activity onboarding path.

active_tasks:
  - issue: 54
    purpose: >-
      Create one coherent Person, Household, weekly ActivitySeries,
      ResponsibilityRule and optional PreparationRequirement transactionally.

planned_tasks:
  - purpose: >-
      Review actual usability after onboarding before authorizing broader CRUD,
      identity mapping or calendar work.

deferred_decisions: []
