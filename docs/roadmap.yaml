schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: first-user-facing-vertical
  purpose: >-
    Build Epic #51's first understandable Drupal application surface on top of
    Epic #35's terminal internal domain foundation.

active_tasks:
  - issue: 52
    purpose: >-
      Render upcoming EffectiveOccurrences, EffectiveResponsibility and
      PreparationEligibility in one read-only frontend view.

planned_tasks:
  - purpose: >-
      After #52 review, decide the smallest governed data-entry/onboarding
      surface needed to make the vertical independently usable.

deferred_decisions: []
