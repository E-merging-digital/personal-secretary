schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: usable-occurrence-exceptions
  purpose: >-
    Make the repeatable first vertical usable when one concrete recurring
    occurrence exceptionally does not happen, without altering its series.

active_tasks:
  - issue: 58
    purpose: >-
      Cancel one exact upcoming base occurrence through the governed
      ActivityException domain path.

planned_tasks:
  - purpose: >-
      Reassess reschedule UX and effective responsibility/preparation semantics
      after cancellation is proven.

deferred_decisions: []
