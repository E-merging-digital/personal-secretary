schema_version: 1

# Planning intent only. GitHub remains the live execution truth.
# Do not store live issue state, PR numbers, commit SHAs, CI state or merge state.

current_goal:
  phase: repeatable-first-vertical
  purpose: >-
    Build repeatable activity creation on top of #54's terminal first-activity
    onboarding without recreating Household or Person context.

active_tasks:
  - issue: 56
    purpose: >-
      Add a weekly ActivitySeries to an existing Household and responsible Person
      with optional preparation through one governed transaction.

planned_tasks:
  - purpose: >-
      Reassess actual usability after multiple-activity creation before
      authorizing edit/cancel/reschedule, identity mapping, calendar or AI.

deferred_decisions: []
